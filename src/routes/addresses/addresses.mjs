import { Router } from "express";

const router = Router();
router.get("/api/addresses", (req, res) => {}); // get addresses
router.post("/api/addresses", (req, res) => {}); // add address
router.put("/api/addresses/:id", (req, res) => {}); // update address
router.delete("/api/addresses/:id", (req, res) => {}); // delete address
export default router;
