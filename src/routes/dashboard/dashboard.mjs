import { Router } from "express";
const router = Router();
router.get("/api/dashboard/summary", (req, res) => {}); // get dashboard summary
router.get("/api/dashboard/revenue", (req, res) => {}); // get revenue data
router.get("/api/dashboard/top-products", (req, res) => {}); // get top products
export default router;